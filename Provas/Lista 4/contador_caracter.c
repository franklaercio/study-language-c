#include <stdio.h>
#include <string.h>

int main() {
    char caracter[40] = "";
    char texto[40] = "";

    int quantidadeRepeticoes = 0;

    fgets(caracter, 40,stdin);
    fgets(texto, 40,stdin);

    for (int i = 0; i < strlen(texto); i++) {
    }

    printf("%s", quantidadeRepeticoes);

    return 0;
}